# AGENTS.md

## 项目身份

- “山河异闻计划 / Project ShanHe”只是开发代号，正式作品名尚未确定。
- 不得把开发代号写成最终书名、出版名或已注册IP名称。
- 正篇主时间轴为1522—1644，包含嘉靖、隆庆、万历、泰昌、天启、崇祯。
- 1644年后的南明与清初只用于余波、番外或人物结局，必须明确标注。

## 仓库布局（现行）

```text
.
├── CLAUDE.md / AGENTS.md / PROJECT_MAP.md / README.md
├── docs/{原则,世界,社会,素材,管理}/     # 设定 00–26·30 · 世界27/29 · 社会28
├── stories/ + catalog.json              # 正文（地脉组 + volumes 卷 + 主章线）
├── web/ · scripts/ · 启动世界观网页.bat
└── archive/                             # 只读历史
```

交叉引用一律写**自仓库根的完整相对路径**（如 `docs/原则/宪法.md`）。`CLAUDE.md` 仅为路标，细则以本文件为准。

## 开始工作前

按顺序阅读：

1. `PROJECT_MAP.md`（或先扫 `CLAUDE.md`）
2. `docs/原则/宪法.md`
3. `docs/原则/00_创作理念.md`
4. 与任务相关的 `docs/**` 设定文件（编号 00—26）
5. 涉及历史、民俗、制度、物价或语言时，读取 `docs/世界/SOURCES.md`
6. 写故事前检查 `docs/管理/12_时间线.md`、`docs/管理/13_伏笔回收.md`、`docs/素材/19_伏笔与未解之谜.md`
7. 已立项故事读取 `stories/<地脉组>/<编号_标题>/` 内文件
8. 发连载 / 排章前读 `docs/原则/24_卷册与连载阅读体验.md`
9. 主章成长边界读 `docs/原则/25`（懂/债/名；现行主角顾晚棠）
10. 人物档案读 `docs/原则/26` 与 `stories/人物志/`；清洛主卷读 `stories/清洛线/中篇总纲.md` 与 **QL00 入卷样板**
11. 河东总编修文档 `docs/原则/30` 仅作历史流程参考；**河东正文已作废**，勿再改河东路径

`archive/` 只保存历史短稿与废弃路径，不是现行设定，不得作为创作依据。

## 不可违反的创作边界

- **异闻：** 不建立可验证的妖鬼谱系；“执念”是创作方法与人物解释，不是作者确认的世界真相。每个怪谈至少保留一种合理解释，作者不宣布最终答案。允许金庸式武与氛围异象；**超常不得替代人物选择**（见 `docs/原则/21` 红灯 5 与 R2）。
- **武学（2026-07-21 校准）：** 允许金庸式武侠——内功/外功/轻身/医毒/奇术，凡武·行武·名武·绝武四层（旁人口径，非经验条）。上限约一人敌军、碎金铁、破小山岗；禁止移山填海、改变大历史、网文境界数字与法力技能树。细则见 `docs/世界/27_武学与奇术总纲.md`。
- **气质：** 先读 `docs/原则/00_创作理念.md` 三句门楣；世界是容器，故事是主体。
- **踏线人：** 沈陌成长仍以懂/债/名为主；武力可偶进，不作每章升级核（见 `docs/原则/25`）。
- 不为了反转而反转，不使用现代网络梗。
- 不把真实历史人物工具化，不改变已发生的大历史。
- 世界大于主角，人物大于设定，情感大于情节机关。

## 史料与原创

- 事实性内容必须标注A/B/C/D/待考等级，定义见 `docs/世界/SOURCES.md`。
- 无可靠来源的历史、民俗、物价、职业规矩和黑话默认【待考】。
- 项目原创必须明确标为D或“项目原创”，不能伪装成史料。
- 资料之间冲突时保留出处和分歧，不擅自拼成唯一版本。

## 文档维护规则

- 00—29 号文件是现行核心文档（27 武学、28 诸地、29 复杂度闸）；存放在 `docs/` 分类子目录中。新增或改名必须同步 `docs/README.md`、`PROJECT_MAP.md`、`README.md` 与 `web/js/constants.js` 卷宗列表。
- 正式正文放在 `stories/<地脉组>/<编号_标题>/正文.md`；人物志在 `stories/人物志/<N_名>/正文.md`（genre N）。分组见 `docs/原则/23`；卷与连载见 `docs/原则/24`；主章线见 `docs/原则/25`；人物志见 `docs/原则/26`；索引更新 `stories/catalog.json`（含 `volumes[].main_path` / `reading_path`，条目 `track` / `review`）。
- 桥头：`stories/桥头震后/` 现行以 **QT00** 为主；**P001《桥上第二碗》2026-07-22 暂撤**，正文在 `archive/stories/桥头震后/P001_桥上第二碗/`，勿当现行正文源。`archive/` 其余为早期短稿与开工草案，同样不得当正文源。
- 修改设定文档时更新头部 Version、更新时间，并在 `CHANGELOG.md` 记录重要变化。
- 创作决策进入 `创作日志.md`；一次性操作过程留在 Git 历史。
- README 面向新人，只写当前事实和使用方法，不写会话流水账。
- **不要**在根目录再铺一层与 `docs/` 重复的编号文档。

## GitHub 与安全

- 当前仓库：`Tera-Dark/shanhe-yiwen`，默认分支 `main`。
- 当前仓库已经公开，用于项目管理、资料校验与后续规划。
- 不得自动修改正式名称、许可范围、维护者、仓库可见性，不得归档或删除仓库。
- 不提交凭据、登录态、私有令牌或未经许可的第三方全文材料。

## 收尾自检

- 新条目优先：`python3 scripts/scaffold_entry.py`（见 `stories/_templates/交稿清单.md`）；**勿**自动塞 `main_path`。
- 若本轮改动了 `stories/**/正文.md`：先运行
  `python3 scripts/postcheck_story.py stories/<地脉组>/<编号_标题>/正文.md`  
  跨章复写：`python3 scripts/check_story_reuse.py`（批量加肉后必跑；也含于 `npm run check`）
  退出码非 0 不得交稿；交稿前建议加 `--strict`。
- 运行 `python3 scripts/check_integrity.py`（或 `npm run check`），确认磁盘↔catalog、三 path、world 路径。
- 运行 `python3 scripts/check_reviews.py`，确认审印登记、报告路径与正文 Version 一致。
- 世界碎片落 `stories/世界/` 并挂 `catalog.world`；禁止只在 `web/app.js` / `web/js/*` 写死业务清单。
- 运行 `git diff --check`。
- 确认 `git status` 没有意外文件。
- 检查 Markdown 文件引用指向真实路径（含 `docs/` 前缀）。
- 检查编号仍为 00—26 连续序列（原则文档）。
- 检查历史与语言新增内容已入 `docs/世界/SOURCES.md` 或标为【待考】。
- 正文不得出现「公文包」等现代包具词；驿递用「油布包/皮囊/报袋」等。词表见 `scripts/postcheck_story.py` 与 `docs/原则/20_故事创作规范.md`「生成后自检」。
- 故事交稿前按 `docs/原则/21_故事审查标准.md` **v0.4**（磁盘唯一法源）做基础审查：**S0 归属** + R0–R3 + 形态卡模块；写入 `07_审查报告.md` 或合订总簿，并回写 `stories/catalog.json` 的 `review`（`standard_version` 须等于 21 文件 Version，当前 `"0.4"`）。**passed*** 硬条件：`round≥1`、`reviewer` 非空、`note` 无「待人工/待写/待勘…」、正文 Version=`source_version`、报告可检索条目 id；禁止仅因 R0 或加肉轮次盖印。无合法 review 一律「待勘」。气质源在 `00` 与 `宪法`，**不另立第三套审查标准**。
- 世界方向：轻玄幻武侠、江湖、小架空、现代概念可化用；勿用单一志异公式卡死所有故事。主线/连载可记世界痕迹一行（`docs/管理/13` 第四节）。
- 内容分层见 `docs/原则/22`；分组见 `docs/原则/23`；卷册连载见 `docs/原则/24`；踏线人主章见 `docs/原则/25`（沈陌跟脚，主/副/其他分槽，成长=懂/债/名）；人物志欲望四问见 `26`。
- 新条目先定**地脉组 / 卷 / track**再定体例；主线占 `Z###` 并挂 `main_path`；正文进组目录。
